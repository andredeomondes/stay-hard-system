# Dia 100 — Mini-Projeto: API REST com Autenticação Completa

## Conceito do Dia

Entrega final: API REST completa com **JWT**, **roles**, **refresh token**, **paginação** e **documentação Swagger**.

---

## Estrutura Final

```
stay-hard-api/
├── src/main/java/br/com/stayhard/
│   ├── config/
│   │   ├── SecurityConfig.java
│   │   ├── CorsConfig.java
│   │   └── OpenApiConfig.java
│   ├── controller/
│   │   ├── AuthController.java
│   │   ├── TarefaController.java
│   │   └── AdminController.java
│   ├── dto/
│   │   ├── request/
│   │   │   ├── RegisterRequest.java
│   │   │   ├── LoginRequest.java
│   │   │   ├── CriarTarefaRequest.java
│   │   │   └── RefreshRequest.java
│   │   └── response/
│   │       ├── AuthResponse.java
│   │       └── TokenResponse.java
│   ├── entity/
│   │   ├── User.java
│   │   ├── Tarefa.java
│   │   └── RefreshToken.java
│   ├── repository/
│   ├── service/
│   │   ├── JwtService.java
│   │   ├── RefreshTokenService.java
│   │   └── CustomUserDetailsService.java
│   ├── security/
│   │   └── JwtAuthenticationFilter.java
│   └── exception/
│       ├── GlobalExceptionHandler.java
│       └── CredenciaisInvalidasException.java
└── src/test/java/
```

---

## Endpoints

```
# Auth (público)
POST /api/auth/register
POST /api/auth/login
POST /api/auth/refresh

# Tarefas (USER)
GET    /api/tarefas
GET    /api/tarefas/{id}
POST   /api/tarefas
PUT    /api/tarefas/{id}
PATCH  /api/tarefas/{id}
DELETE /api/tarefas/{id}

# Admin (ADMIN)
GET    /api/admin/usuarios
DELETE /api/admin/usuarios/{id}
```

---

## Testar Fluxo Completo

```bash
# 1. Registrar
curl -X POST http://localhost:8080/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{"email":"admin@test.com","senha":"admin123","nome":"Admin"}'

# 2. Login
TOKEN=$(curl -s -X POST http://localhost:8080/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"admin@test.com","senha":"admin123"}' | jq -r '.token')

# 3. Usar token
curl http://localhost:8080/api/tarefas \
  -H "Authorization: Bearer $TOKEN"

# 4. Criar tarefa
curl -X POST http://localhost:8080/api/tarefas \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"nome":"Estudar Spring Security","categoria":"ESTUDO","prioridade":"ALTA"}'
```

---

## Entrega

Commit `stay-hard-api-v2-security` no GitHub.

---

## Verificação

- [ ] JWT funciona?
- [ ] Roles são respeitadas?
- [ ] Refresh token funciona?
- [ ] Swagger documenta tudo?
