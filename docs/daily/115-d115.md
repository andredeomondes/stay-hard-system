# Dia 115 — React: Formulários com React Hook Form

## Conceito do Dia

Gerenciar formulários em React é complexo. **React Hook Form** simplifica com performance e validação.

---

## Onde Estudar

- [Docs: React Hook Form](https://react-hook-form.com/)
- [Video: RHF Tutorial](https://youtu.be/bUqnK2Hw3yA)

---

## Checklist de Código

- [ ] Instalar react-hook-form
- [ ] Criar formulário com validação
- [ ] Mostrar erros de validação

---

## Setup

```bash
npm install react-hook-form
```

## Formulário Básico

```jsx
import { useForm } from 'react-hook-form';

function LoginForm() {
    const { register, handleSubmit, formState: { errors, isSubmitting } } = useForm();
    
    const onSubmit = async (data) => {
        console.log(data); // { email: "...", senha: "..." }
        await login(data.email, data.senha);
    };
    
    return (
        <form onSubmit={handleSubmit(onSubmit)}>
            <div>
                <label>Email</label>
                <input 
                    type="email"
                    {...register('email', { 
                        required: 'Email é obrigatório',
                        pattern: {
                            value: /^[A-Z0-9._%+-]+@[A-Z0-9.-]+\.[A-Z]{2,}$/i,
                            message: 'Email inválido'
                        }
                    })}
                />
                {errors.email && <span className="erro">{errors.email.message}</span>}
            </div>
            
            <div>
                <label>Senha</label>
                <input 
                    type="password"
                    {...register('senha', {
                        required: 'Senha é obrigatória',
                        minLength: {
                            value: 6,
                            message: 'Mínimo 6 caracteres'
                        }
                    })}
                />
                {errors.senha && <span className="erro">{errors.senha.message}</span>}
            </div>
            
            <button type="submit" disabled={isSubmitting}>
                {isSubmitting ? 'Entrando...' : 'Entrar'}
            </button>
        </form>
    );
}
```

## Validação Customizada

```jsx
const { register, watch } = useForm({
    defaultValues: {
        nome: '',
        categoria: 'ESTUDO',
        prioridade: 'MEDIA'
    }
});

// Obter valor atual de um campo
const categoria = watch('categoria');
```

---

## Verificação

- [ ] Validação funciona antes do submit?
- [ ] isSubmitting previne double-click?
