# Dia 123 — Docker: Multi-stage Build

## Conceito do Dia

**Multi-stage builds** reduzem o tamanho da imagem final separando build de produção.

---

## Onde Estudar

- [Docs: Multi-stage](https://docs.docker.com/build/building/multi-stage/)

---

## Checklist de Código

- [ ] Criar build multi-stage
- [ ] Otimizar tamanho da imagem
- [ ] Usar .dockerignore

---

## .dockerignore

```
.git
.gitignore
target/
node_modules/
*.md
.env
docker-compose.yml
Dockerfile
```

## Multi-stage Dockerfile

```dockerfile
# ====================
# Stage 1: Build
# ====================
FROM maven:3.9-eclipse-temurin-21 AS builder

WORKDIR /app

# Copiar só o que precisa para build
COPY pom.xml .
RUN mvn dependency:go-offline -B

COPY src ./src
RUN mvn clean package -DskipTests

# ====================
# Stage 2: Runtime
# ====================
FROM eclipse-temurin:21-jre-alpine

WORKDIR /app

# Usuário não-root (segurança)
RUN addgroup -S appgroup && adduser -S appuser -G appgroup
USER appuser

# Copiar só o JAR do stage anterior
COPY --from=builder /app/target/*.jar app.jar

EXPOSE 8080

ENTRYPOINT ["java", "-XX:+UseContainerSupport", "-jar", "app.jar"]
```

## Comparação de tamanho

```
Build simples:    ~800MB (JDK + Maven + source)
Multi-stage:      ~200MB (JRE only)
Alpine variant:   ~150MB (musl libc)
```

## Front-end com Multi-stage

```dockerfile
# Build stage
FROM node:20-alpine AS builder

WORKDIR /app
COPY package*.json ./
RUN npm ci
COPY . .
RUN npm run build

# Runtime stage
FROM nginx:alpine

COPY --from=builder /app/dist /usr/share/nginx/html
COPY nginx.conf /etc/nginx/conf.d/default.conf

EXPOSE 80

CMD ["nginx", "-g", "daemon off;"]
```

---

## Verificação

- [ ] Imagem final < 300MB?
- [ ] Container inicia corretamente?
