# Dia 174 — Entrevista: Preparação Técnica

## Conceito do Dia

Preparação para entrevistas técnicas: algoritmos, estrutura de dados, e system design.

---

## Tópicos Importantes

### Algoritmos
- Sorting (Quick, Merge, Heap)
- Search (Binary, BFS, DFS)
- Graph algorithms
- Dynamic Programming básico

### Estruturas de Dados
- Arrays e Lists
- Maps e Sets
- Trees e Graphs
- Stacks e Queues

### Java Specific
- Collections internals
- Concurrency (threads, synchronized)
- JVM (garbage collection, memory model)
- Streams e lambdas

## Sites para Prática

| Site | Foco |
|------|------|
| LeetCode | Algoritmos |
| HackerRank | Interviews |
| Exercism | Java specific |
| Codewars | Katas |

## Preparação Diária

```java
// Exemplo: Two Sum (LeetCode 1)
public int[] twoSum(int[] nums, int target) {
    Map<Integer, Integer> seen = new HashMap<>();
    
    for (int i = 0; i < nums.length; i++) {
        int complement = target - nums[i];
        if (seen.containsKey(complement)) {
            return new int[]{seen.get(complement), i};
        }
        seen.put(nums[i], i);
    }
    
    return new int[]{-1, -1};
}
```

## Perguntas Frequentes Java

```markdown
1. Qual a diferença entre ArrayList e LinkedList?
   - ArrayList: acesso O(1), inserção O(n)
   - LinkedList: inserção O(1), acesso O(n)

2. Como funciona o Garbage Collection?
   - G1GC, zonas Eden/Survivor/Old
   - Mark and Sweep

3. O que é JPA/Hibernate?
   - ORM que mapeia objetos para tabelas

4. Explique Injeção de Dependência
   - Spring cria e injeta dependências automaticamente
```

---

## Verificação

- [ ] Faz 2-3 problemas por dia?
- [ ] Conhece conceitos Java profundamente?
