# Dia 122 — Docker: Docker Compose

## Conceito do Dia

**Docker Compose** orchestra múltiplos containers (app + banco + redis) com um arquivo YAML.

---

## Onde Estudar

- [Docs: Docker Compose](https://docs.docker.com/compose/)
- [Video: Compose Tutorial](https://youtu.be/MVIIyX_WaVM)

---

## Checklist de Código

- [ ] Criar docker-compose.yml
- [ ] Configurar API + PostgreSQL
- [ ] docker-compose up/down

---

## docker-compose.yml

```yaml
version: '3.8'

services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    environment:
      - SPRING_PROFILES_ACTIVE=prod
      - DB_HOST=db
      - DB_PORT=5432
      - DB_NAME=stayhard
      - DB_USER=postgres
      - DB_PASSWORD=postgres123
    depends_on:
      db:
        condition: service_healthy
    networks:
      - stayhard-network

  db:
    image: postgres:16-alpine
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_DB=stayhard
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres123
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - stayhard-network

volumes:
  postgres_data:

networks:
  stayhard-network:
    driver: bridge
```

## Comandos Compose

```bash
# Subir todos os serviços
docker-compose up -d

# Ver logs
docker-compose logs -f api

# Rebuild after changes
docker-compose up -d --build

# Parar e remover
docker-compose down

# Parar e remover volumes
docker-compose down -v

# Executar comando em container
docker-compose exec db psql -U postgres
```

---

## Verificação

- [ ] API conecta ao banco?
- [ ] Volumes persistem dados?
