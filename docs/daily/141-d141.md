# Dia 141 — IA: Introdução a LLMs

## Conceito do Dia

**LLMs** (Large Language Models) são modelos treinados em texto massivo. GPT, Claude, Gemini são exemplos.

---

## Onde Estudar

- [Docs: OpenAI API](https://platform.openai.com/docs)
- [Artigo: How LLMs Work](https://blog.research.google/2023/08/01/how-llms-work/)

---

## Checklist de Código

- [ ] Entender como LLMs funcionam
- [ ] Configurar API key
- [ ] Fazer primeira chamada

---

## Conceitos

| Termo | Significado |
|-------|-------------|
| Prompt | Instrução para o modelo |
| Token | Unidade de texto (≈4 chars) |
| Temperature | Criatividade (0=factual, 1=criativo) |
| System prompt | Contexto/regra para o assistente |

## OpenAI API

```bash
# Install
pip install openai

# Or Java
<dependency>
    <groupId>com.theokanning.openai-gpt3-java</groupId>
    <artifactId>service</artifactId>
    <version>0.18.2</version>
</dependency>
```

## Exemplo Java

```java
public class OpenAIClient {
    
    private final String apiKey;
    private final OkHttpClient client;
    
    public OpenAIClient(String apiKey) {
        this.apiKey = apiKey;
        this.client = new OkHttpClient();
    }
    
    public String chat(String prompt) throws IOException {
        String json = """
            {
                "model": "gpt-4o",
                "messages": [
                    {"role": "user", "content": "%s"}
                ],
                "temperature": 0.7
            }
            """.formatted(prompt);
        
        RequestBody body = RequestBody.create(json, MediaType.get("application/json"));
        Request request = new Request.Builder()
            .url("https://api.openai.com/v1/chat/completions")
            .header("Authorization", "Bearer " + apiKey)
            .post(body)
            .build();
        
        try (Response response = client.newCall(request).execute()) {
            String responseBody = response.body().string();
            // Parse JSON response
            return extractContent(responseBody);
        }
    }
}
```

## Preços (aproximados)

| Modelo | Input | Output |
|--------|-------|--------|
| GPT-4o | $5/1M | $15/1M |
| GPT-4o-mini | $0.15/1M | $0.60/1M |
| Claude 3 Haiku | $0.25/1M | $1.25/1M |

---

## Verificação

- [ ] API responde?
- [ ] Custo está dentro do esperado?
