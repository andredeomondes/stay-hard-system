# Dia 104 — JavaScript: Fetch API e Async/Await

## Conceito do Dia

**Fetch API** permite fazer requisições HTTP do JavaScript. Com **async/await**, código assíncrono fica limpo.

---

## Onde Estudar

- [Tutorial: Fetch](https://developer.mozilla.org/en-US/docs/Web/API/Fetch_API)
- [Artigo: Async/Await](https://developer.mozilla.org/en-US/docs/Learn/JavaScript/Asynchronous/Promises)

---

## Checklist de Código

- [ ] Fazer GET com fetch
- [ ] Fazer POST com JSON
- [ ] Tratar erros com try/catch

---

## GET Request

```javascript
const API_URL = 'http://localhost:8080/api/v1';

async function listarTarefas() {
    try {
        const response = await fetch(`${API_URL}/tarefas`, {
            headers: {
                'Authorization': `Bearer ${localStorage.getItem('token')}`
            }
        });
        
        if (!response.ok) {
            throw new Error(`HTTP error! status: ${response.status}`);
        }
        
        const tarefas = await response.json();
        return tarefas;
        
    } catch (error) {
        console.error('Erro ao buscar tarefas:', error);
        throw error;
    }
}
```

## POST Request

```javascript
async function criarTarefa(dados) {
    try {
        const response = await fetch(`${API_URL}/tarefas`, {
            method: 'POST',
            headers: {
                'Content-Type': 'application/json',
                'Authorization': `Bearer ${localStorage.getItem('token')}`
            },
            body: JSON.stringify(dados)
        });
        
        if (!response.ok) {
            const erro = await response.json();
            throw new Error(erro.mensagem || 'Erro ao criar');
        }
        
        return await response.json();
        
    } catch (error) {
        console.error('Erro:', error);
        throw error;
    }
}
```

## Uso prático

```javascript
// Carregar tarefas ao iniciar
document.addEventListener('DOMContentLoaded', async () => {
    try {
        const tarefas = await listarTarefas();
        renderizarTarefas(tarefas);
    } catch (e) {
        alert('Erro ao carregar tarefas');
    }
});

// Criar tarefa
btnAdd.addEventListener('click', async () => {
    const tarefa = await criarTarefa({
        nome: inputNome.value,
        categoria: 'ESTUDO',
        prioridade: 'ALTA'
    });
    
    // Atualizar lista
    const tarefas = await listarTarefas();
    renderizarTarefas(tarefas);
});
```

---

## Verificação

- [ ] Fetch faz requisições?
- [ ] Erros são tratados?
