# Dia 120 — Mini-Projeto: Stay Hard React Completo

## Conceito do Dia

Entrega final: aplicação React completa com autenticação, CRUD, filtros e visual profissional.

---

## Estrutura Final

```
stay-hard-react/
├── src/
│   ├── main.jsx
│   ├── App.jsx
│   ├── api/
│   │   └── client.js
│   ├── components/
│   │   ├── Button.jsx
│   │   ├── Card.jsx
│   │   ├── Input.jsx
│   │   └── Layout.jsx
│   ├── context/
│   │   └── AuthContext.jsx
│   ├── hooks/
│   │   ├── useAuth.js
│   │   └── useTarefas.js
│   ├── pages/
│   │   ├── LoginPage.jsx
│   │   ├── RegisterPage.jsx
│   │   ├── DashboardPage.jsx
│   │   └── TarefasPage.jsx
│   ├── styles/
│   │   ├── GlobalStyles.js
│   │   └── theme.js
│   └── utils/
│       └── api.js
├── index.html
└── vite.config.js
```

---

## Funcionalidades

- [ ] Login/Registro com React Hook Form
- [ ] JWT em localStorage
- [ ] Protected Routes
- [ ] CRUD completo com React Query
- [ ] Filtros por categoria
- [ ] Loading states
- [ ] Error handling
- [ ] Animações com Framer Motion (opcional)

---

## API Client

```jsx
// api/client.js
const API_URL = 'http://localhost:8080/api/v1';

async function request(endpoint, options = {}) {
    const token = localStorage.getItem('token');
    
    const config = {
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...(token && { Authorization: `Bearer ${token}` }),
            ...options.headers
        }
    };
    
    const response = await fetch(`${API_URL}${endpoint}`, config);
    
    if (!response.ok) {
        const erro = await response.json().catch(() => ({}));
        throw new Error(erro.mensagem || 'Erro na requisição');
    }
    
    if (response.status === 204) return null;
    return response.json();
}

export const api = {
    login: (email, senha) => request('/auth/login', { method: 'POST', body: { email, senha } }),
    register: (email, senha, nome) => request('/auth/register', { method: 'POST', body: { email, senha, nome } }),
    listarTarefas: () => request('/tarefas'),
    criarTarefa: (dados) => request('/tarefas', { method: 'POST', body: dados }),
    atualizarTarefa: (id, dados) => request(`/tarefas/${id}`, { method: 'PUT', body: dados }),
    deletarTarefa: (id) => request(`/tarefas/${id}`, { method: 'DELETE' })
};
```

---

## Testar

```bash
npm run dev
# http://localhost:5173
```

---

## Entrega

Commit `stay-hard-react-v1` no GitHub.

---

## Verificação

- [ ] Fluxo completo funciona?
- [ ] React Query gerencia cache?
- [ ] Styled Components funcionam?
