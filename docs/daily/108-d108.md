# Dia 108 — JavaScript: Login e Auth no Front-end

## Conceito do Dia

Implementar fluxo de autenticação no front-end: login, registro, logout, e proteção de rotas.

---

## Onde Estudar

- [Tutorial: JWT Auth Flow](https://developer.mozilla.org/en-US/docs/Web/API/Web_Storage_API)

---

## Checklist de Código

- [ ] Tela de login/registro
- [ ] Salvar token no localStorage
- [ ] Redirecionar se não autenticado

---

## API Client com Auth

```javascript
class StayHardAPI {
    constructor(baseUrl) {
        this.baseUrl = baseUrl;
        this.token = localStorage.getItem('token');
    }
    
    getHeaders() {
        const headers = { 'Content-Type': 'application/json' };
        if (this.token) {
            headers['Authorization'] = `Bearer ${this.token}`;
        }
        return headers;
    }
    
    async login(email, senha) {
        const response = await fetch(`${this.baseUrl}/api/auth/login`, {
            method: 'POST',
            headers: { 'Content-Type': 'application/json' },
            body: JSON.stringify({ email, senha })
        });
        
        if (!response.ok) {
            const erro = await response.json();
            throw new Error(erro.mensagem || 'Login falhou');
        }
        
        const data = await response.json();
        this.token = data.token;
        localStorage.setItem('token', data.token);
        localStorage.setItem('usuario', JSON.stringify(data));
        
        return data;
    }
    
    async register(email, senha, nome) {
        const response = await fetch(`${this.baseUrl}/api/auth/register`, {
            method: 'POST',
            headers: { 'Content-Type': 'application/json' },
            body: JSON.stringify({ email, senha, nome })
        });
        
        if (!response.ok) {
            throw new Error('Registro falhou');
        }
        
        return this.login(email, senha);
    }
    
    logout() {
        this.token = null;
        localStorage.removeItem('token');
        localStorage.removeItem('usuario');
    }
    
    isAuthenticated() {
        return !!this.token;
    }
}
```

## Tela de Login

```html
<div id="auth-screen" class="auth-container">
    <div class="auth-card">
        <h1>🔥 Stay Hard</h1>
        <p>Gerencie suas tarefas e seja consistente!</p>
        
        <form id="login-form">
            <input type="email" id="email" placeholder="Email" required>
            <input type="password" id="senha" placeholder="Senha" required>
            <button type="submit" class="btn btn-primary">Entrar</button>
        </form>
        
        <p class="auth-footer">
            Não tem conta? <a href="#" id="show-register">Cadastre-se</a>
        </p>
    </div>
</div>
```

```javascript
document.getElementById('login-form').addEventListener('submit', async (e) => {
    e.preventDefault();
    
    const email = document.getElementById('email').value;
    const senha = document.getElementById('senha').value;
    
    try {
        await api.login(email, senha);
        mostrarTelaPrincipal();
    } catch (e) {
        alert(e.message);
    }
});
```

## Proteger Rotas

```javascript
function requireAuth() {
    if (!api.isAuthenticated()) {
        mostrarTelaLogin();
        return false;
    }
    return true;
}

// Em cada tela protegida
function mostrarTelaPrincipal() {
    if (!requireAuth()) return;
    
    document.getElementById('auth-screen').style.display = 'none';
    document.getElementById('main-screen').style.display = 'block';
    carregarTarefas();
}
```

---

## Verificação

- [ ] Login funciona e salva token?
- [ ] Logout limpa sessão?
- [ ] Rotas protegidas redirecionam?
