# Dia 91 — Spring Security: Introdução

## Conceito do Dia

**Spring Security** é o framework de autenticação e autorização do ecossistema Spring. Protege sua API de acessos não autorizados.

---

## Onde Estudar

- [Docs: Spring Security](https://docs.spring.io/spring-security/reference/index.html)
- [Video: Security Basics](https://youtu.be/smykn2k7yA8)

---

## Checklist de Código

- [ ] Adicionar dependência security
- [ ] Configurar auth básica
- [ ] Entender filter chain

---

## Dependência

```xml
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-security</artifactId>
</dependency>
```

## Configuração básica

```java
@Configuration
@EnableWebSecurity
public class SecurityConfig {
    
    @Bean
    SecurityFilterChain filterChain(HttpSecurity http) throws Exception {
        http
            .csrf(csrf -> csrf.disable())
            .authorizeHttpRequests(auth -> auth
                .requestMatchers("/api/public/**").permitAll()
                .requestMatchers("/swagger-ui/**", "/api-docs/**").permitAll()
                .requestMatchers("/h2-console/**").permitAll()
                .anyRequest().authenticated()
            )
            .httpBasic(basic -> {})
            .sessionManagement(session -> session
                .sessionCreationPolicy(SessionCreationPolicy.STATELESS)
            );
        
        return http.build();
    }
}
```

## Comportamento padrão

Com security, TODA requisição requer autenticação. A resposta:

```
HTTP/1.1 401 Unauthorized
WWW-Authenticate: Basic realm="Realm"
```

---

## Verificação

- [ ] /api/public/** funciona sem auth?
- [ ] /api/** retorna 401?
