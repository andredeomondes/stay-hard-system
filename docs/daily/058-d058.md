# Dia 58 — Sessão e Cookies

## Conceito do Dia

**HTTP é stateless** - cada requisição é independente. Para manter estado (login), usamos:
- **Cookies**: dados armazenados no browser
- **Sessão**: dados mantidos no servidor

---

## Onde Estudar

- [Artigo: HttpSession](https://www.baeldung.com/httpsession)
- [Artigo: Cookies vs Sessions](https://stackoverflow.com/questions/3352184/difference-between-cookie-and-session)

---

## Checklist de Código

- [ ] Criar login com sessão
- [ ] Armazenar dados na sessão
- [ ] Invalidar sessão no logout

---

## Login com Sessão

```java
@WebServlet("/login")
public class LoginServlet extends HttpServlet {
    
    @Override
    protected void doPost(HttpServletRequest req, HttpServletResponse resp) 
            throws ServletException, IOException {
        
        String email = req.getParameter("email");
        String senha = req.getParameter("senha");
        
        // Validar credenciais
        if (autenticar(email, senha)) {
            // Criar sessão
            HttpSession session = req.getSession(true);
            session.setAttribute("usuario", email);
            session.setMaxInactiveInterval(30 * 60); // 30 minutos
            
            resp.sendRedirect("/dashboard");
        } else {
            resp.sendRedirect("/login?erro=true");
        }
    }
}
```

## Proteger páginas com Filter

```java
@WebFilter("/dashboard/*")
public class ProtecaoFilter implements Filter {
    
    @Override
    public void doFilter(ServletRequest req, ServletResponse resp, FilterChain chain) 
            throws IOException, ServletException {
        
        HttpServletRequest httpReq = (HttpServletRequest) req;
        HttpSession session = httpReq.getSession(false);
        
        if (session == null || session.getAttribute("usuario") == null) {
            ((HttpServletResponse) resp).sendRedirect("/login");
            return;
        }
        
        chain.doFilter(req, resp);
    }
}
```

## Logout

```java
@WebServlet("/logout")
public class LogoutServlet extends HttpServlet {
    
    @Override
    protected void doPost(HttpServletRequest req, HttpServletResponse resp) 
            throws IOException {
        
        HttpSession session = req.getSession(false);
        if (session != null) {
            session.invalidate();
        }
        
        resp.sendRedirect("/login");
    }
}
```

---

## Verificação

- [ ] Usuário não logado é redirecionado para login?
- [ ] Sessão expira após inatividade?
