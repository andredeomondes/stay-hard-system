# Dia 164 — Testing: Performance Testing com k6

## Conceito do Dia

**k6** é ferramenta de load testing com scripts em JavaScript. Simula milhares de usuários.

---

## Onde Estudar

- [Docs: k6](https://k6.io/docs/)
- [Video: k6 Tutorial](https://youtu.be/hcP8Lj市场和)

---

## Instalação

```bash
# macOS
brew install k6

# Linux
sudo gpg --no-default-keyring --keyring /usr/share/keyrings/k6-archive-keyring.gpg \
  --keyserver hkp://keyserver.ubuntu.com:80 --recv-keys C5AD17C747E3415A3642D57D77C6C491D6AC1D69

# Windows: download do site
```

## Script básico

```javascript
// load-test.js
import http from 'k6/http';
import { check, sleep } from 'k6';

export const options = {
    stages: [
        { duration: '30s', target: 20 },
        { duration: '1m', target: 50 },
        { duration: '30s', target: 0 },
    ],
    thresholds: {
        http_req_duration: ['p(95)<500'],
        http_req_failed: ['rate<0.01'],
    },
};

const BASE_URL = 'http://localhost:8080';

export default function () {
    // Login
    const loginRes = http.post(`${BASE_URL}/api/auth/login`, 
        JSON.stringify({ email: 'test@test.com', senha: 'test123' }),
        { headers: { 'Content-Type': 'application/json' } }
    );
    
    check(loginRes, {
        'login status 200': (r) => r.status === 200,
    });
    
    const token = loginRes.json('token');
    
    // Listar tarefas
    const listRes = http.get(`${BASE_URL}/api/tarefas`, {
        headers: { 'Authorization': `Bearer ${token}` }
    });
    
    check(listRes, {
        'list status 200': (r) => r.status === 200,
        'list has tarefas': (r) => r.json('content') !== undefined,
    });
    
    sleep(1);
}
```

## Executar

```bash
# Teste normal
k6 run load-test.js

# Com relatório HTML
k6 run --out html=report.html load-test.js

# Teste smoke (quick)
k6 run --vus 5 --duration 10s load-test.js
```

---

## Verificação

- [ ] Load test executa?
- [ ] Thresholds passam?
