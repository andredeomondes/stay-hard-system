# Dia 179 — DevOps: Chaos Engineering

## Conceito do Dia

**Chaos Engineering** testa resiliência propositalmente causando falhas.

---

## Ferramentas

| Tool | Uso |
|------|-----|
| Gremlin | Chaos comercial |
| Chaos Monkey | Netflix (simples) |
| Litmus | Kubernetes |
| Toxiproxy | Network chaos |

## Chaos Monkey (Spring)

```xml
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-chaos-starter</artifactId>
</dependency>
```

## Cenários

```markdown
1. Matar instâncias aleatoriamente
2. Simular latência de rede
3. Injetar erros HTTP 500
4. Consumir memória/CPU
5. Matar conexões de banco
```

## Implementação

```java
@Component
public class ChaosService {
    
    @Value("${chaos.enabled:false}")
    private boolean enabled;
    
    public void maybeInjectLatency() {
        if (!enabled) return;
        
        double random = Math.random();
        if (random < 0.1) { // 10% chance
            try {
                Thread.sleep((long)(Math.random() * 2000));
            } catch (InterruptedException e) {
                Thread.currentThread().interrupt();
            }
        }
    }
    
    public void maybeInjectError() {
        if (!enabled) return;
        
        if (Math.random() < 0.01) { // 1% chance
            throw new SimulatedException("Chaos injection");
        }
    }
}
```

## GitHub Actions

```yaml
- name: Chaos Test
  run: |
    # Run during off-peak hours
    curl -X POST http://chaos-service/inject \
      -d '{"type":"latency","duration":"30s","target":"api"}'
    
    # Verify system recovers
    k6 run load-test.js
```

---

## Verificação

- [ ] Sistema se recupera de falhas?
- [ ] SLOs são mantidos?
