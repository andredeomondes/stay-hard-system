# Dia 67 — Spring Boot: Exception Handling

## Conceito do Dia

Trate exceções de forma centralizada com `@ControllerAdvice` e `@ExceptionHandler`. Uma única classe trata TODAS as exceções da aplicação.

---

## Onde Estudar

- [Artigo: @ControllerAdvice](https://www.baeldung.com/global-error-handling)
- [Artigo: Exception Hierarchy](https://www.baeldung.com/spring-boot-exception-handling)

---

## Checklist de Código

- [ ] Criar exceções customizadas
- [ ] Criar classe de erro padrão
- [ ] Implementar @ControllerAdvice

---

## Exceções Customizadas

```java
public class RecursoNaoEncontradoException extends RuntimeException {
    public RecursoNaoEncontradoException(String recurso, Long id) {
        super(recurso + " com ID " + id + " não encontrado");
    }
}

public class ValidacaoException extends RuntimeException {
    private final List<String> erros;
    
    public ValidacaoException(String mensagem) {
        super(mensagem);
        this.erros = List.of(mensagem);
    }
    
    public ValidacaoException(List<String> erros) {
        super(String.join(", ", erros));
        this.erros = erros;
    }
    
    public List<String> getErros() { return erros; }
}
```

## DTO de Erro

```java
public record ErroResposta(
    int status,
    String mensagem,
    LocalDateTime timestamp,
    String caminho,
    List<String> detalhes
) {}
```

## Handler Global

```java
@RestControllerAdvice
@RequiredArgsConstructor
public class GlobalExceptionHandler {
    
    @ExceptionHandler(RecursoNaoEncontradoException.class)
    @ResponseStatus(HttpStatus.NOT_FOUND)
    public ErroResposta handleNaoEncontrado(RecursoNaoEncontradoException ex) {
        return new ErroResposta(
            404,
            ex.getMessage(),
            LocalDateTime.now(),
            "/api/tarefas",
            null
        );
    }
    
    @ExceptionHandler(ValidacaoException.class)
    @ResponseStatus(HttpStatus.BAD_REQUEST)
    public ErroResposta handleValidacao(ValidacaoException ex) {
        return new ErroResposta(
            400,
            "Erro de validação",
            LocalDateTime.now(),
            "/api/tarefas",
            ex.getErros()
        );
    }
    
    @ExceptionHandler(MethodArgumentNotValidException.class)
    @ResponseStatus(HttpStatus.BAD_REQUEST)
    public ErroResposta handleBeanValidation(MethodArgumentNotValidException ex) {
        List<String> erros = ex.getBindingResult()
            .getFieldErrors()
            .stream()
            .map(e -> e.getField() + ": " + e.getDefaultMessage())
            .toList();
        
        return new ErroResposta(
            400,
            "Validação falhou",
            LocalDateTime.now(),
            "/api/tarefas",
            erros
        );
    }
    
    @ExceptionHandler(Exception.class)
    @ResponseStatus(HttpStatus.INTERNAL_SERVER_ERROR)
    public ErroResposta handleGeral(Exception ex) {
        return new ErroResposta(
            500,
            "Erro interno do servidor",
            LocalDateTime.now(),
            "/api/tarefas",
            null
        );
    }
}
```

---

## Verificação

- [ ] 404 para recurso inexistente?
- [ ] 400 para dados inválidos?
