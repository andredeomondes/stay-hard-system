# Dia 151 — SaaS: Multi-tenancy

## Conceito do Dia

**Multi-tenancy** permite servir múltiplos clientes (tenants) com uma única instância da aplicação. Cada tenant vê apenas seus dados.

---

## Onde Estudar

- [Artigo: Multi-tenancy](https://docs.microsoft.com/en-us/azure/azure-sql/database/designing-multi-tenant-saas-apps)
- [Artigo: JPA Tenant](https://vladmihalcea.com/jpa-multiple-schema/)

---

## Estratégias

| Estratégia | Prós | Contras |
|------------|------|---------|
| Database por tenant | Isolamento total | Custo alto |
| Schema por tenant | Isolamento, menos custoso | Complexidade |
| Coluna tenant_id | Mais simples, menor custo | Isolamento parcial |

## Implementação (Coluna tenant_id)

```java
@Entity
@Table(name = "tarefas")
public class Tarefa {
    
    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;
    
    @Column(nullable = false)
    private String tenantId;
    
    @Column(nullable = false)
    private String nome;
    
    @ManyToOne(fetch = FetchType.LAZY)
    @JoinColumn(name = "usuario_id", nullable = false)
    private Usuario usuario;
    
    // Query automático por tenant
    @PrePersist
    public void prePersist() {
        if (tenantId == null) {
            tenantId = SecurityContext.getCurrentTenant();
        }
    }
}
```

## Query Filter Global

```java
@Configuration
public class TenantFilter {
    
    @Bean
    public JpaSpecificationExecutor<Tarefa> tarefaSpecifications() {
        return new JpaSpecificationExecutor<Tarefa>() {
            // Implementação que adiciona tenant filter
        };
    }
}
```

## Spring Security com Tenant

```java
@Service
@RequiredArgsConstructor
public class TenantContext {
    
    private static final ThreadLocal<String> currentTenant = new ThreadLocal<>();
    
    public static String getCurrentTenant() {
        return currentTenant.get();
    }
    
    public static void setCurrentTenant(String tenant) {
        currentTenant.set(tenant);
    }
}

@Component
public class TenantInterceptor extends HandlerInterceptorAdapter {
    
    @Override
    public boolean preHandle(HttpServletRequest request, HttpServletResponse response, Object handler) {
        String tenantId = extractTenantFromRequest(request);
        TenantContext.setCurrentTenant(tenantId);
        return true;
    }
    
    @Override
    public void afterCompletion(HttpServletRequest request, HttpServletResponse response, 
                                Object handler, Exception ex) {
        TenantContext.clear();
    }
}
```

---

## Verificação

- [ ] Tenant é isolado corretamente?
- [ ] Queries filtram por tenant?
