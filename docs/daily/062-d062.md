# Dia 62 — Spring Boot: @RestController e @RequestMapping

## Conceito do Dia

Anotações fundamentais do Spring MVC:
- `@RestController`: combina `@Controller` + `@ResponseBody`
- `@RequestMapping`: mapeia URLs para métodos

---

## Onde Estudar

- [Docs: Spring MVC](https://docs.spring.io/spring-boot/docs/current/reference/html/web.html#web.servlet.spring-mvc)
- [Artigo: @RequestMapping](https://www.baeldung.com/spring-requestmapping)

---

## Checklist de Código

- [ ] Criar CRUD completo com @GetMapping, @PostMapping, etc
- [ ] Usar @PathVariable para URLs dinâmicas
- [ ] Usar @RequestBody para receber JSON

---

## TarefaController

```java
@RestController
@RequestMapping("/api/tarefas")
@RequiredArgsConstructor
public class TarefaController {
    
    private final TarefaService service;
    
    @GetMapping
    public List<Tarefa> listar() {
        return service.listarTodas();
    }
    
    @GetMapping("/{id}")
    public Tarefa buscarPorId(@PathVariable Long id) {
        return service.buscarPorId(id);
    }
    
    @PostMapping
    @ResponseStatus(HttpStatus.CREATED)
    public Tarefa criar(@RequestBody TarefaDTO dto) {
        return service.criar(dto);
    }
    
    @PutMapping("/{id}")
    public Tarefa atualizar(@PathVariable Long id, @RequestBody TarefaDTO dto) {
        return service.atualizar(id, dto);
    }
    
    @DeleteMapping("/{id}")
    @ResponseStatus(HttpStatus.NO_CONTENT)
    public void deletar(@PathVariable Long id) {
        service.deletar(id);
    }
}
```

## DTO (Data Transfer Object)

```java
public record TarefaDTO(
    String nome,
    String categoria,
    String prioridade
) {}
```

---

## Verificação

- [ ] GET /api/tarefas retorna lista?
- [ ] POST cria e retorna 201?
- [ ] @PathVariable funciona?
