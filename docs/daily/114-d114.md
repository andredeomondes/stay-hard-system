# Dia 114 — React: useContext e Estado Global

## Conceito do Dia

**Context** compartilha dados entre componentes sem passar props manualmente por cada nível.

---

## Onde Estudar

- [Docs: Context](https://react.dev/reference/react/createContext)
- [Artigo: useContext](https://react.dev/reference/react/useContext)

---

## Checklist de Código

- [ ] Criar AuthContext
- [ ] Criar TarefaContext
- [ ] Usar useContext nos componentes

---

## AuthContext

```jsx
// context/AuthContext.jsx
import { createContext, useContext, useState, useEffect } from 'react';

const AuthContext = createContext(null);

export function AuthProvider({ children }) {
    const [user, setUser] = useState(null);
    const [token, setToken] = useState(() => localStorage.getItem('token'));
    
    useEffect(() => {
        if (token) {
            const storedUser = JSON.parse(localStorage.getItem('usuario') || 'null');
            setUser(storedUser);
        }
    }, [token]);
    
    const login = async (email, senha) => {
        const response = await api.login(email, senha);
        setToken(response.token);
        setUser({ email: response.email, nome: response.nome });
        localStorage.setItem('token', response.token);
        localStorage.setItem('usuario', JSON.stringify(response));
    };
    
    const logout = () => {
        setToken(null);
        setUser(null);
        localStorage.removeItem('token');
        localStorage.removeItem('usuario');
    };
    
    return (
        <AuthContext.Provider value={{ user, token, login, logout, isAuthenticated: !!token }}>
            {children}
        </AuthContext.Provider>
    );
}

export const useAuth = () => useContext(AuthContext);
```

## Uso

```jsx
// App.jsx
import { AuthProvider } from './context/AuthContext';

function App() {
    return (
        <AuthProvider>
            <TarefaProvider>
                <Router />
            </TarefaProvider>
        </AuthProvider>
    );
}

// Header.jsx
import { useAuth } from '../context/AuthContext';

function Header() {
    const { user, logout } = useAuth();
    
    return (
        <header>
            <h1>Stay Hard</h1>
            {user && (
                <div>
                    <span>Olá, {user.nome}</span>
                    <button onClick={logout}>Sair</button>
                </div>
            )}
        </header>
    );
}
```

---

## Verificação

- [ ] Context disponível em toda a árvore?
- [ ] logout limpa estado global?
